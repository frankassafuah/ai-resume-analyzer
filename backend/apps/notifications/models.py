"""Models for user notifications and delivery.

Concrete models arrive with their milestone. Extend `BaseModel`
(UUID pk + timestamps) or `SoftDeleteModel` for retained PII.
"""
# from apps.common.models import BaseModel, SoftDeleteModel
